{
  "name": "save-as-root-on-remote",
  "displayName": "save as root on remote",
  "description": "Use sudo to save files on a remote connection when using Remote - SSH",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.70.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:save-as-root-on-remote.isRemote"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "save-as-root-on-remote.isRemote",
      "title": "Is Remote"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.70.0",
    "@types/glob": "^7.2.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "16.x",
    "eslint": "^8.20.0",
    "glob": "^8.0.3",
    "mocha": "^10.0.0",
    "typescript": "^4.7.4",
    "@vscode/test-electron": "^2.1.5"
  }
}
